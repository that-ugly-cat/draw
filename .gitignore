# Secrets and local state
.env
.env.bak-*
data/

# SPEC.md does not belong in a public repository: it holds the failure-mode
# analysis and the discarded decisions. Whoever clones from scratch does not
# have it and has no way of noticing — which is why CLAUDE.md says so out loud.
SPEC.md

# Environment and build artefacts
.venv/
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
build/
dist/
